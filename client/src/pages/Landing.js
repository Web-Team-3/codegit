import React from 'react';
import Navbar from '../hoc/header';

export default function Landing(props) {
    return (
        <div id="landing-container">
            <Navbar/>
        </div>
    );
}